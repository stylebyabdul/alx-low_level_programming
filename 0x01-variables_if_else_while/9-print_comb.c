#include <stdio.h>

/**
 * main - Prints all possible combination of single-digit numbers.
 *
 * Return: Always 0.
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		putchar((% of 9 ) + 0);
		if (num == 9)
			continue;
		putchar(',');
		putcahr(' ');
	}
	putchar('\n');
	return (0);
}

