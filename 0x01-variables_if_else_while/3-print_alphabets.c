#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, and then in uppercase.
 *
 * Result: Always 0.
 */
int main(void)
{
	char letter;

	for (letter = 'a' && 'A'; letter <= 'z' && 'Z'letter++)
	{
		putchar(letter)
	}
	putchar('\n');
	return (0);
}
